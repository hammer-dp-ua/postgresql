docker build -t postgres-image .

docker run --restart always --detach --name postgres-container --publish 5432:5432 -v "z:\USER\PostgreSQL\my-postgres.conf":/etc/postgresql/postgresql.conf postgres-image -c config_file=/etc/postgresql/postgresql.conf

docker stop postgres-container
docker rm postgres-container
docker exec -it --user postgres postgres-container bash